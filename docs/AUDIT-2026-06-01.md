# 심층 감사 — 2026-06-01 (coaching-log + coach-finder + 공유 데이터레이어)

> 5개 에이전트 병렬 정독 + 헤드라인 P0 3건 메인 직접 재확인 (오탐 0). P0~P2 백로그의 출처.
> 본 사본은 coaching-log 레포용 (동일 문서가 coach-finder 에도 존재).

---

## 총평

두 제품 모두 "잘 굴러가는 1차본". **AI 추출 파이프라인**과 **Supabase RLS 설계**는 전문가급. 단 운영 전 **P0 보안 3건** + **공유 `coaches_directory` 계약 3중 복사 드리프트**가 최대 리스크.

---

## 🔴 P0 (검증 완료)

### P0-1. coaching-log `/api/extract-session` 무인증 + `CORS *`
- `api/extract-session.js:93` JWT 검증 없음. 아무 출처에서나 transcript POST → Gemini 할당량 소진.

### P0-2. coach-finder `/api/coaches` 무인증 + PII 노출
- `api/coaches.ts:44` 인증 0줄 + `email`·`phone`·`gender`·`location` select. 주석은 정반대로 "PII off the wire". `/recommend*`·`/parse-pdf`·`/similar-coaches` 도 무인증 → 익명 Gemini 비용.

### P0-3. coach-finder `tools/firebase-sa.json` GCP 개인키 평문 + AuthGuard `isAuthReady` 미사용 (`App.tsx:54`).

> 부가: `api_consumers.rate_limit_per_day` 미강제.

---

## coaching-log 상세

**유지**: `api/extract-session.js` 3-pass 프롬프트 + 잘림복구 파서(`:587-654`) + Pro→Flash 폴백 / RLS(SECURITY DEFINER 헬퍼 + PM 격리 phase_d3) / `bp_on_won` 트리거.

**개선(P1)**:
- `api/extract-session.js` 인증 · CORS 제한.
- `coach_applications` anon INSERT 방어 (`20260515_phase_j:91`) — rate limit/captcha.
- **`public/index.html` 12,090줄 모듈화 착수** — `<script type="module">` 분리, 940줄 `renderBPDetailBody`(`:10887`) 부터. ~87 `innerHTML` 이 `escHtml` "기억해서 호출"에 의존 → 한 곳만 빠져도 stored XSS.

**개선(P2)**: 문서 정정(README/HANDOVER 모델명 · `api/match-coaches.js` 없음 · 마이그레이션 28) / `created_by IS NULL` 백필(phase_d3 는 경고만) / 임베딩 차원 주석 정정(OpenAI→Gemini).

**제거**: `server.js`·`lib/csv-manager.js`·`lib/backup.js` + express/cors/node-cron/dotenv (Vercel 死, node-cron 안 돎) / 死 `GEMINI_KEY=''`(`extract-session.js:29`) / `index.html:5194` 死 DDL.

**추가**: extract→save + RLS 매트릭스 자동 테스트(현 0건) / 평가→tier 자동 트리거(Gap 4) / 코치 사진 Firebase→Supabase 이전(HANDOVER §8.3).

---

## coach-finder 상세 (요약)

**유지**: AI 추천 3-tier 폴백 · v1 admin authz(JWT→getUser→role) · release-check/smoke.
**개선**: 내부 엔드포인트 인증 / PII 분리 / AuthGuard isAuthReady / God 컴포넌트 분해 / export 이미지 타임아웃.
**제거**: `python-service/`(死) · `server/index.ts`(dev-only) · 死코드 다수.

---

## ⭐ 공유 데이터레이어 (최대 구조 이슈)

- **S2 — `coaches_directory` 계약 3곳 손복사, drift**: coach-finder `supabaseAdmin.ts` / ud-ops `supabase-source.ts`(4컬럼 누락) / coaching-log `index.html`. → [공유 계약 문서](contracts/coaches-directory.md)(본 레포 원본) 가 1차 대응. CI 일치 검사 후속.
- **S1 — ud-ops 미러가 코치 없는 프로젝트**: `business_plan_coaches` 미러 안 함 → `bp_on_won` 복사 대상 없음.
- **S4 — 분리된 신원**: Supabase(uuid) vs ud-ops NextAuth(cuid). ud-ops `created_by` admin 하드코딩 → PM 스코프 RLS 가 진짜 PM 에게서 숨김.
- **역할 모델**: 공유 DB = admin/pm/coach. ud-ops 6역할 미공유.
- **평가 UI 중복**(Gap 3): 같은 `coach_evaluations` 두 스키마.

---

## 우선순위 종합

| 순위 | 항목 | 위치 | 브리프 |
|------|------|------|--------|
| P0 | extract-session 인증 + CORS | coaching-log | SEC1 |
| P0 | 내부 엔드포인트 인증 + PII 분리 + 키 회전 + AuthGuard | coach-finder | (coach-finder SEC1~3·F1) |
| P1 | coach_applications anon INSERT 방어 | coaching-log | SEC2 |
| P1 | index.html 모듈화 착수 | coaching-log | H1 |
| P1 | coaches_directory 공유 계약 단일화 + CI | 3 레포 | D1 |
| P2 | 死코드 제거 + 문서 정정 | coaching-log | D1·DOCS1 |
