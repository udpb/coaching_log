# AUDIT-2026-06-20 — 종합 정리 감사 (데드 컬럼·데드 코드·스키마 드리프트·문서 stale)

> 사용자 요청(2026-06-15): "한 번 싹 정리 — 나중에 필요한 건지, 이력 변경으로 더 이상 필요 없는
> 데드코드/잔재, 문서 갱신 여부." HANDOFF 가 예고한 "AUDIT-2026-06-15 후보" 의 실제 산출물.
> 작성: 메인 세션(Curator·Guardian·Historian) · 2026-06-20 · 4개 차원 병렬 조사 + 메인 교차검증.
>
> **방법:** 4개 Explore 에이전트(데드컬럼·데드코드·스키마정합·문서stale)가 file:line 근거 수집 →
> 메인이 핵심 주장(라인 번호·카운트·CSS 사용여부·헬퍼 생존)을 직접 재검증(에이전트 오기 정정 포함).
> **변경 없음** — 본 문서는 진단 + 결정 제안. 실제 수정은 사용자 결정 후 브리프로 집행.

---

## TL;DR — 4개 결론

| 차원 | 결론 | 조치 |
|------|------|------|
| ⓐ "죽은" 컬럼 | ❌ 오판 정정(2026-06-20): 죽은 게 아니라 **coach-finder 핵심 기능에 실사용**(추천 엔진 RPC·코치 카드). coaching-log 만 0참조. | **보존 확정 — ADR-024** (드롭 안 함, 소유=coach-finder 문서화) |
| ⓑ 데드 코드 | 런타임 구 status 어휘 **0건**(깨끗). 단 **dead CSS 9선택자**(bp-status-*/bp-trans-*) + `_esc()` 컨벤션 드리프트(45곳). `is_pm_of_project()` 는 **생존**(HANDOFF 오기 정정). | dead CSS 제거 권장 · `_esc` 후순위 |
| ⓒ 스키마 정합 | **모순 0 · 정합**. 제로베이스 재생 한계는 phase_z 에 문서화됨(의도적). 라이브 대조 SQL 체크리스트 제공. | 라이브 검증 SQL 사용자 실행 |
| ⓓ 문서 stale | **다수 stale** — CLAUDE.md(38→42 파일) · README(18→22 필드) · HANDOVER/HISTORY(구 won 어휘·줄수·마이그 카운트) · PRD/INTEGRATED(won). | **갱신 권장**(명확한 정리 승수) |

---

## ⓐ "죽은" 컬럼/테이블 — 실제로는 coach-finder 소유 기능 (오판 정정 2026-06-20)

> ✅ **결론(ADR-024):** 아래 컬럼들은 coaching-log 가 0참조라 처음엔 "미구현/폐기 후보"로 봤으나,
> **coach-finder 코드 직접 조사 결과 전부 실사용**(추천 엔진 RPC·코치 카드 등)임이 확인됐다. → **보존 확정**,
> 소유=coach-finder 로 계약문서에 명문화. **드롭/물리분리 안 함.** 아래 "미구현/호출 0" 서술은 *coaching-log
> 기준*이며 coach-finder 기준으로는 실사용임에 유의.

본 레포는 **두 제품(coaching-log + coach-finder)의 스키마 SoT**다. coaching-log 코드가 안 읽는 컬럼이라도
coach-finder 가 쓰는 공유 컬럼일 수 있다(아래가 그 경우). 무조건 삭제는 **공유 계약 위반 + coach-finder 파괴** 위험.

### A. coach-finder 전용 기능 컬럼 (coaching-log 0참조 · coach-finder 실사용 — 보존, ADR-024)
| 컬럼 | 테이블 | 추가 마이그 | 의도(주석) | 현황 |
|------|--------|-----------|-----------|------|
| `embedding` (vector 1536) | coaches_directory | 20260424_phase4e | RAG 코치 추천(PM향) | RPC `search_coaches_by_embedding()` 정의만, 호출 0 |
| `embedding_source_hash` | coaches_directory | phase4e | 임베딩 무효화 추적 | 미사용 |
| `embedding_updated_at` | coaches_directory | phase4e | 신선도 | 미사용 |
| `embedding_model` | coaches_directory | phase4e | 모델 감사기록 | 미사용 (⚠️ 주석 "OpenAI" 오기) |
| `inferred_skills` (text[]) | coaches_directory | 20260504_phase_e4 | 로그→자동 스킬추론 | 외부 도구 미연동, 호출 0 |
| `inferred_skills_updated_at` | coaches_directory | phase_e4 | 신선도 | 미사용 |
| `inferred_skills_source` | coaches_directory | phase_e4 | gemini\|frequency | 미사용 |
| `roles_capable` (text[]) | coaches_directory | 20260523_phase_p | 보유 역량 역할 | GIN 인덱스만, coaching-log 호출 0 |
| `roles_active_2026` (text[]) | coaches_directory | phase_p | 2026 활동 의향 | coaching-log 호출 0 |
| `ud_programs` (text[]) | coaches_directory | 20260523_phase_q | 참여 UD 프로그램 | coaching-log 호출 0 |

→ **확인된 coach-finder 실사용처:** `embedding`+`search_coaches_by_embedding` RPC = 추천 엔진
(coach-finder `api/_lib/recommend.ts:261`, `/api/recommend`). `inferred_skills`·`roles_capable`·
`roles_active_2026`·`ud_programs` = `api/_lib/supabaseAdmin.ts`(SELECT·매핑) + `ud_programs` 는
`CoachCard.tsx:103-110` 렌더. → 전부 **생존(coach-finder)**, 보존 확정(ADR-024).

### B. coach-finder 도메인 (본 레포 코드는 안 쓰지만 coach-finder 가 씀 — 삭제 금지)
- `business_plan_coaches`: `payment_info`(jsonb) · `task_summary`(text)
- `business_plans`: `client` · `total_budget` · `kickoff_count` · `task_program_learning` ·
  `task_review_participation` · `task_program_attendance` · `task_participant_type` ·
  `task_report_frequency` · `payment_schedule` · `installment_midpay_note` · `installment_balance_note`
  (계약서 생성·결제 옵션 — coach-finder/계약생성기 소유)
- `business_plans.legacy_firestore_id`: Firestore→Supabase 1회성 이관 키(외부 스크립트)
- `api_consumers` · `api_consumer_usage` 테이블: API 키 관리(admin 전용, 별도 패널)

→ 이들은 **공유 계약**. 삭제하려면 coach-finder 동기화 + ADR + 마이그레이션 필요. **현 상태 유지가 정답.**

### C. 생존 확인 (오탐 방지 — 본 레포가 실제 사용)
`required_kpis`(ADR-022) · `extraction_model`/`extraction_version`(phase_aa) ·
`is_business`/`business_number`/`business_name`(계약정보 모달) · `business_plan_id`(트리거 링크) ·
`onboarded_at`(온보딩 게이트) · `coach_id`(phase_z SoT). — 전부 생존.

### ✅ ⓐ 해결 (ADR-024, 2026-06-20)
coach-finder 실사용 확인 → **보존 확정.** 계약문서 v2 에 소유=coach-finder(🔵) 표식 추가. 드롭/물리분리 안 함
(진짜 테이블 분리는 RPC 재작성·재임베딩 동반 → 핸드오프 후 별도 ADR 후보). embedding 모델 = Gemini 1536
(마이그 "OpenAI" 주석 오기, 계약문서가 정정 권위).

---

## ⓑ 이력 변경 데드코드/잔재

### B1. 런타임 구 status 어휘 — ✅ 깨끗 (0건)
`public/index.html` · `api/*.js` 에서 won/lost/draft/proposed/bp_on_won 문자열·분기 **런타임 사용 0**.
(마이그레이션 파일의 과거 기록·주석은 이력 보존 대상 — 정상.) ADR-023 코드 반영 완전.

### B2. Dead CSS — ✅ 제거 완료 (2026-06-20, 브리프 CLEAN-20260620)
`public/index.html` 의 사업 status 배지 CSS가 **정의만 있고 동적 할당 0** 이었음 → 23줄 제거 완료(베이스
`.bp-status`+변형5+`.bp-trans` 블록). 이하 진단 기록:
- `.bp-status-active`/`-draft`/`-proposed`/`-won`/`-lost`/`-cancelled` (~1290 블록)
- `.bp-detail-trans button.bp-trans-won`/`:hover`/`.bp-trans-lost` (~1362)

→ grep `bp-status-`/`bp-trans-` = **9건 전부 CSS 정의부, 마크업 할당 0**. coaching-log 는 사업 status
배지를 렌더하지 않음(그건 coach-finder UI) → **전체 dead CSS**. 제거해도 기능·RLS 무영향.

### B3. `_esc()` DEPRECATED 위임 래퍼 — 🟡 후순위
`_esc()` 45곳(대부분 `_reportPageHtml()` 보고서 생성) · `escapeHtml()` 17곳 잔존. 둘 다 `escAttr` 위임이라
**XSS 안전**하나 신규 금지 컨벤션과 불일치. 기능 영향 0 → 향후 리팩터 시 `escAttr()` 통일 권장(지금 급하지 않음).

### B4. `is_pm_of_project()` — ✅ 생존 (HANDOFF 오기 정정)
HANDOFF 가 "死 헬퍼" 로 의심했으나, `20260503_phase_d3` RLS 정책 **6곳**(projects·coaching_logs·
coach_evaluations select/insert/update/delete)에서 사용 중. **변경 금지 헬퍼**(CLAUDE.md). 死 아님.

### B5. field-defs 중앙화 잔재 — ✅ 없음
index.html·api 에 STRUCTURED_FIELD_KEYS/VALUE_RULES 구 하드코딩 잔재 0. ADR-020 완전 준수.

---

## ⓒ 스키마 SoT 드리프트 — ✅ 정합 (모순 0)

42개 마이그레이션 시간순 정독 결과 **모순/누락 0**. 핵심:
- status 라이프사이클이 phase5b(5값)→phase_f(8값 혼재)→phase_ac(won+active 과도기)→
  phase_ad(데이터 변환)→**phase_ae(4값 정본 planning/active/completed/cancelled)** 로 수렴. 최종 승자 명확.
- 트리거 최종형: `bp_lifecycle_sync_ins`(AFTER INSERT, active+project_id NULL) ·
  `bp_lifecycle_sync_upd`(AFTER UPDATE, status 변동+active+NULL) ·
  `bp_status_propagate_upd`(completed→closed/cancelled→archived). 함수 `handle_business_plan_won()` ·
  `handle_business_plan_terminal()`. 모두 멱등(DROP IF/IF NOT EXISTS/CREATE OR REPLACE).
- RLS 헬퍼 5종 + `can_access_coach_contract_info()` 전부 안정.

### 제로베이스 재생 한계 (문서화됨 · 의도적)
`20260610_phase_z` 헤더가 명시: phase4a(0421)가 `coach_id = auth.uid()` 정책을 만드는데 coach_id 는
phase_z(0610)에서야 추가됨 → 빈 DB 시간순 재생 시 phase4a 실패. "적용 마이그 수정 금지" 원칙상
phase4a 못 고침. **라이브는 IF NOT EXISTS 로 무영향**, 제로베이스 재구축만 별도 절차 필요. = 버그 아님.

### 🔷 라이브 검증 SQL (사용자 실행 — 메인은 Supabase 접근 불가)
```sql
-- 1) status 4값만 (won/draft/proposed/lost = 0 이어야)
SELECT status, count(*) FROM public.business_plans GROUP BY status ORDER BY status;
-- 2) active 인데 project 없는 행 0
SELECT count(*) FROM public.business_plans WHERE status='active' AND project_id IS NULL;
-- 3) business_plan_id NULL 인 projects (HANDOFF "bp없는 세션有 projects" 확인)
SELECT id, name, status FROM public.projects WHERE business_plan_id IS NULL;
-- 4) status CHECK 가 4값인지
SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname='business_plans_status_check';
-- 5) 트리거 3종 존재
SELECT tgname FROM pg_trigger WHERE tgrelid='public.business_plans'::regclass AND NOT tgisinternal;
```

---

## ⓓ 문서 stale — 갱신 권장 (명확한 정리 승수)

> CLAUDE.md/AGENTS.md/glossary 는 06-15 status 어휘 정정 완료. 단 **마이그 카운트·줄수 등 숫자**와
> **HANDOVER/HISTORY/README/PRD/INTEGRATED** 는 미갱신. (메인 교차검증으로 라인·수치 확정.)

### 1순위 (정합성 직결)
| 파일:라인 | stale | 정본 |
|----------|-------|------|
| `CLAUDE.md:97` | "스키마 SoT (38 파일)" | **42 파일** |
| `README.md:24` | "fills 18 structured fields" | **22 fields** |
| `docs/HANDOVER.md` (status 섹션·검증 매트릭스 다수) | won/bp_on_won 트리거·status 8값·29개 마이그 | ADR-023: 4값·`bp_lifecycle_sync_*`·42개 |
| `docs/HISTORY.md:26,39` | 마이그 "(28)" · index.html "12,090줄" | **42** · **10,882줄** |

### 2순위 (수치/어휘)
| 파일 | stale | 정본 |
|------|-------|------|
| `CLAUDE.md` 스택표 | index.html "~10,600줄" · "22필드 규칙"(혼재) | **~10,882줄** |
| `docs/HANDOVER.md` 파일트리 | `lib/`·`server.js` 잔재(HISTORY:24 가 이미 플래그) | 2026-06-01 CLEAN 에서 삭제됨 |
| `docs/PRD-v2.md`/`.html` | 수주(won)·`bp_on_won`(2026-06-04 작성, ADR-023 이전) | 헤더에 "ADR-023 이후 status=planning/active/completed/cancelled" 주석 |
| `docs/INTEGRATED_ARCHITECTURE.md` | `bp_on_won`·status='won' gap 서술 | gap 해소(ADR-023) 표기 |

### 3순위 (사소)
- "OpenAI" 오기: `phase4e` 마이그 주석(이력·수정금지) · `ARCHITECTURE.md:406`(로드맵 기록) ·
  `contracts/coaches-directory.md`(glossary 이미 플래그). 정본 = Gemini/1536. 마이그 주석은 불변 원칙상 보존.

---

## 📌 결정 결과 (2026-06-20)

1. **ⓐ coach-finder 소유 컬럼**: ✅ **보존**(ADR-024) — coach-finder 실사용. 계약문서 v2 소유 표식. 드롭 안 함.
2. **ⓑ dead CSS 9선택자**: ✅ **제거 완료**(CLEAN-20260620, 23줄).
3. **ⓓ 문서 stale 갱신**: ✅ **완료** — 수치 정정(CLAUDE/README/HISTORY/AGENTS) + 과거 문서 ADR-023 배너/이동.
4. **ⓒ 라이브 검증 SQL 5종**: 🔷 사용자 Supabase 실행 대기(특히 #3 bp없는 projects).
5. `_esc`/`escapeHtml`→escAttr 통일: 핸드오프 정리 Phase 4 에서 진행.

---

## 부록 — 본 감사가 정정한 가설
- ❌ "is_pm_of_project() 死 헬퍼"(HANDOFF) → ✅ 생존(RLS 6곳). 
- ❌ index.html "10,222줄"(에이전트 D 오기) → ✅ **10,882줄**(메인 wc 확인).
- ❌ "죽은 컬럼 11개"(HANDOFF) → ✅ 정정: 죽은 게 아니라 **coach-finder 실사용**(추천 엔진·코치 카드). 보존(ADR-024).
